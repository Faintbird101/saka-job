name: jobhunter
description: Manage the automated job application pipeline.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # add as you go:
  # dio: ^5.x            (http client)
  # flutter_riverpod: ^2.x  (state)
  # go_router: ^14.x     (navigation)
  # freezed_annotation / json_annotation (models)

dev_dependencies:
  flutter_test:
    sdk: flutter
  # build_runner, freezed, json_serializable

flutter:
  uses-material-design: true
